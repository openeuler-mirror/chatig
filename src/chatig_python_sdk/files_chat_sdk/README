# File Chat SDK

`file_chat_sdk` 是 ChatIG 项目的 Python SDK，用于调用服务端文件对话接口：
- **上传文件**：`POST /v1/files`
- **基于文件的对话补全**：`POST /v1/file/completions`

该 SDK 封装了调用方式，兼容 **OpenAI Chat Completions** 的请求/响应结构，支持上传文档、绑定文档上下文、多轮问答。

---

## 环境参数

SDK 的行为可通过以下环境变量控制：

| 变量名               | 默认值                    | 说明                                   |
|----------------------|---------------------------|----------------------------------------|
| `CHATIG_BASE_URL`    | `http://127.0.0.1:8000`   | ChatIG 服务端基础地址                  |
| `CHATIG_API_KEY`     | 空                         | API Key（如服务端要求鉴权时设置）      |
| `CHATIG_TIMEOUT`     | `60` (秒)                 | HTTP 请求超时时间                      |

---

## 功能特性

- **标准接口封装**
  - `/v1/files` → `FileChatClient.upload_files()`
  - `/v1/file/completions` → `FileChatClient.file_completions()`

- **兼容 OpenAI Chat 格式**
  - 请求消息 `messages=[{"role": "user", "content": "..."}]`
  - 响应包含 `choices[0].message.content`

- **文件上下文对话**
  - 支持在 `file_completions` 中传入 `doc_ids`，与上传文件绑定，实现文档级对话

- **批量上传**
  - `upload_files()` 支持同时上传多个文件（PDF、TXT 等）

- **健康检查**
  - `FileChatClient.health_check()` 调用服务端 `/health` 检测可用性

- **参数控制**
  - `temperature`、`top_p`、`max_tokens`、`stop` 等采样参数与 OpenAI 接口一致

---

## 使用方法

### 1. 安装依赖
```bash
pip install requests pydantic


## 请求 / 响应示例

### 1. 上传文件

**请求：**
```bash
curl -X POST http://127.0.0.1:8000/v1/files \
  -F "files=@example.txt" \
  -F "scene=test"


curl -X POST http://127.0.0.1:8000/v1/file/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "chatchat",
    "doc_ids": ["doc_12345"],
    "messages": [
      {"role": "user", "content": "请帮我总结这份文档的要点"}
    ],
    "temperature": 0.2,
    "max_tokens": 256
  }'
